#include "main.h"
/**
 * add - check for lower case letters
 * @n: input
 * @c: input
 *Return: 1 if c is lowercase
 * 0 if c is anyhting else.
 */
int add(int n, int c)
{
	int r = n + c;

	return (r);
}
